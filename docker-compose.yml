services:
  tomsliv_ai:
    build: .     
    image: softvence/tomsliv_ai:latest  
    container_name: tomsliv_ai_container 
    ports:
      - "8000:8000"               
    env_file:
      - .env                          
    restart: always                  